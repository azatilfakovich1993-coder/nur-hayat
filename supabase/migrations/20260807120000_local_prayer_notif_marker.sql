-- Раньше телефон (локальные будильники) и сервер (check-prayers, FCM push)
-- независимо слали одно и то же уведомление о намазе — гарантированный дубль
-- на каждое событие для Android. Телефон теперь отмечается здесь, что сам
-- успешно поставил уведомления на сегодня — сервер это проверяет и не шлёт
-- повторно для намазов (азкары не трогаем — для них локального будильника
-- вообще нет, они полностью зависят от сервера).
alter table prayer_schedules
  add column if not exists local_scheduled_date date;
